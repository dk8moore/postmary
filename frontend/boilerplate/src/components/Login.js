import React from 'react';

const Login = () => {
    return (
        <div className="login-container">
            <h2>Login</h2>
            <a href="/auth/login/google-oauth2/">Login with Google</a>
        </div>
    );
};

export default Login;